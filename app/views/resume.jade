.resume
  .sidebar.top

  .sidebar.bottom
    .area(scroll, ng-show='visible', ng-class="{alt: !activeFilter()}")
      h3 {{resume.name}}
      .content
        h4 {{resume.careerTitle}}
        h4 {{resume.location}}
    .area.alt(ng-if='activeFilter()')
      h3 Active Filter
      div
        div(ng-if='role')
          b Role:
          span {{tagLabel(role)}}
          a(ng-click='filter("role", role)') Remove

        div(ng-if='skill')
          b Skill:
          span {{tagLabel(skill)}}
          a(ng-click='filter("skill", skill)') Remove

        .content(ng-if="filteredProjects")
          h4 Related Projects:
          ul
            li(ng-repeat='project in filteredProjects')
              a(ng-click="goto(project.name)") {{project.name}}


    //.area.alt
    //  h3 View
    //  ul.inline-items.content
    //    li Resume
    //    li: a Coversheet
    .area
      h3 Personal Notes
      p You can leave notes here and they will persist between sessions.

  h1 {{resume.name}}
  h2(ng-if='resume.careerTitle') {{resume.careerTitle}}

  article
    resume-section(label='Summary')
      ul
        li(ng-repeat='item in resume.summary') {{item}}

    resume-section(label='Roles')
      .area
        .two-columns
          tags-list(category='primary', label='Primary', type='role')

        .two-columns
          tags-list(category='secondary', label='Secondary', type='role')


    resume-section(label='Skills')
      .area
        tags-list(category='primary', label='Primary', ul-class='two-columns', type='skill')

      .area
        tags-list(category='secondary', label='Secondary', ul-class='inline-items', type='skill')

      .area
        tags-list(category='legacy', label='Throwback', ul-class='inline-items', type='skill')


    resume-section(label='Projects')
      h4.header @Work
      team(ng-repeat='team in resume.teams | filter:{personal: false}', team='team')

      h4.header @Play
      team(ng-repeat='team in resume.teams | filter:{personal: true}', team='team')

    resume-section(label='Interests', ng-if='resume.interests')
      .area(ng-repeat='area in resume.interests')
        h4 {{area.category}}
        ul.inline-items
          li(ng-repeat='item in area.items') {{item}}

    resume-section(label='Goals', ng-if='resume.goals')
      .area(ng-repeat='area in resume.goals')
        h4 {{area.category}}
        ul
          li(ng-repeat='item in area.items') {{item}}

    resume-section(label='Articles', ng-if='resume.articles')
      .area(ng-repeat='area in resume.articles')
        h4(ng-if="area.category") {{area.category}}
        ul
          li(ng-repeat='item in area.items')
            a(href="{{item.href}}") {{item.name}}

    //resume-section(label='Critique')
    //  .area
    //    h4 My Top 3 Strongest Traits
    //    ul.inline-items
    //        li Creative Problem Solver
    //        li Hard Worker
    //        li Fast Learner
    //
    //  .area
    //    h4 My Top 3 Weakest Traits
    //    ul.inline-items
    //        li Easy to Manage
    //        li Strong life/work balance
    //        li Highly Focused

    resume-section(label='Social')
      ul.inline-items
          li: a Codewars
          li: a Stack Overflow
          li: a Quora
          li: a Github
          li: a Codepen
          li: a Twitter





